## v1.2.1

### AI Settings
- Added **Check Balance** button — queries `/user/balance` to show account balance (supports DeepSeek and other OpenAI-compatible providers)
- Added **Check Online** button — opens DeepSeek usage page in browser (only shown when using DeepSeek API)

### Functions Tab
- Delete button moved to left sidebar — hover over a function item to reveal the delete button
- Hotkey row now has its own full-width layout (no longer squeezed)
- "Show in Tray Menu" toggle now has its own row with description text
- Added "View function examples" link at bottom of sidebar, linking to `functions.md`

### Other
- Added `functions.md` to repository documenting all default function groups and prompts
